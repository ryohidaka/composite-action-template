name: Bump Uses

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  bump-uses:
    runs-on: ubuntu-latest
    steps:
      - name: Bump Uses
        uses: ryohidaka/action-bump-uses@v0.4.0
